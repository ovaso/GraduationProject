<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="top.irises.pbox3">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.AddKey"></activity>
        <activity android:name=".activities.SettingsActivity" />
        <activity android:name=".activities.AddCont" />
        <activity android:name=".activities.AddPay" />
        <activity android:name=".activities.AddAssign" />
        <activity android:name=".activities.InfoActivity" />
        <activity android:name=".activities.KeychainActivity"
            android:windowSoftInputMode="adjustNothing"
            />
        <activity
            android:name=".activities.BienvenueActivity"
            android:theme="@style/AppTheme.Common" />
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/AppTheme.SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:theme="@style/AppTheme.Main" />
    </application>

</manifest>